# WeatherAI API key — create one at https://weather-ai.co (Dashboard -> API Keys)
# Keys are prefixed "wai_". Without this, /api/weather and /api/risk return a
# 500 explaining the key is missing; the front-end simulation still works.
WEATHERAI_API_KEY=wai_your_key_here
